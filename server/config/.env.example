# ===========================================
# COLLAB-O ENVIRONMENT VARIABLES
# ===========================================

# Database Configuration
MONGODB_URI=your_mongodb_connection_string_here

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure

# Server Configuration
PORT=5000
CLIENT_URL=http://localhost:5173

# Google Gemini AI Configuration
GEMINI_API_KEY=your_google_gemini_api_key_here

# Razorpay Configuration (existing)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# PayPal Configuration (NEW - for contracts)
# Get these from: https://developer.paypal.com/dashboard/
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_client_secret_here
PAYPAL_MODE=sandbox
# Use 'sandbox' for testing (no real money)
# Use 'live' for production (real transactions)

# ===========================================
# SETUP INSTRUCTIONS
# ===========================================

# 1. MongoDB:
#    - Get connection string from MongoDB Atlas
#    - Example: mongodb+srv://username:password@cluster.mongodb.net/collab-o

# 2. JWT Secret:
#    - Generate a random secure string
#    - Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# 3. Gemini API:
#    - Get from: https://makersuite.google.com/app/apikey
#    - Used for AI quiz, course, and contract generation

# 4. Razorpay (existing payment system):
#    - Get from: https://dashboard.razorpay.com/app/keys
#    - Used for project payments

# 5. PayPal (NEW - for milestone payments):
#    - Go to: https://developer.paypal.com/dashboard/
#    - Click "Create App" under REST API apps
#    - Copy Client ID and Secret
#    - Start with SANDBOX mode for testing
#    - Create test accounts at: https://developer.paypal.com/dashboard/accounts
#    - Switch to LIVE mode when deploying to production

# ===========================================
# PAYPAL SANDBOX TESTING
# ===========================================
# 1. Create sandbox test accounts (buyer and seller)
# 2. Use sandbox credentials in this file
# 3. Set PAYPAL_MODE=sandbox
# 4. Test payments use fake money
# 5. Login details available in PayPal dashboard

# ===========================================
# PRODUCTION DEPLOYMENT
# ===========================================
# 1. Get production PayPal credentials
# 2. Set PAYPAL_MODE=live
# 3. Update CLIENT_URL to your production domain
# 4. Use production MongoDB cluster
# 5. Generate new JWT_SECRET for production
